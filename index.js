// arrays, objetos
let meta = {
    value: 'ler um livro por mes',
    checked: false,
    log: (info) => {
        console.log(info)
    }
}

console.log(meta.value);

//function        //arrow function
const criarMeta = () => {}

function criarMeta() {
    
}